{% extends 'base.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    {{ form_row(form.designation) }}
    <input type="submit">
    {{ form_end(form) }}
    <a href="{{ path('compte_liste') }}">retour</a>
{% endblock %}
